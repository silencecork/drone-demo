pipeline:
  install:
    image: node:10.15
    commands:
      - node -v
      - npm -v
      - npm install
      - npm install -g mocha
      - mocha test/api.js

  scp:
    image: appleboy/drone-scp
    host: 
      - 13.230.240.54
    port: 22
    secrets:
      - source: user_name
        target: scp_username
      - source: key
        target: scp_key
    rm: true
    target: /home/ec2-user/data/drone-demo
    source: 
      - "*.*"
      - "test/*.*"
      - "node_modules"
      #- "*.js"
      #- "*.json"
      #- "test/*.json"
    when:
      branch: master
      local: true

  run:
    image: appleboy/drone-ssh
    host: 13.230.240.54
    username: ec2-user
    secrets: 
      - source: key
        target: ssh_key
    script:
      - echo "kill all node service"
      - cd /home/ec2-user/data/drone-demo
      - killall node
      - echo "start service..."
      - node index.js
      - exit 0    